;;; Theme --- darksmile theme config.
;;; Commentary:
;;; Code:
(load-theme 'gruvbox t)

(custom-set-faces
 '(default ((t (:family "SourceCodePro" :height 105)))))

(provide 'theme)
;;; theme.el ends here
